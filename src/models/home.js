export default class User {
  constructor(date , burnedCal, consumedCal, suggestedCal ) {
    this.date = date
    this.burnedCal = burnedCal;
    this.consumedCal = consumedCal;
    this.suggestedCal = suggestedCal;
  }
}